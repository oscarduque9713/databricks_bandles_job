# A sample job for test_bandles.

resources:
  pipelines:
    etl_pipeline:
      name: etl_pipeline
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: ../src/etl_pipeline

      libraries:
        - glob:
            include: ../src/etl_pipeline/transformations/**